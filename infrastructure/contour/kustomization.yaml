apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: contour
resources:
  - namespace.yaml
  - release.yaml
configMapGenerator:
  - name: contour-values
    namespace: contour
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
