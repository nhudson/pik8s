apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: nhudson+pik8sle@nhudson.dev
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - dns01:
        cloudflare:
          email: nick.hudson@gmail.com
          apiKeySecretRef:
            name: cloudflare-api-key-secret
            key: api-key
